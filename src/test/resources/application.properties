# No broker in a test run. Without these, every context that starts spends its time retrying a
# connection to localhost:9092. Nothing here is asserted on: the listeners only translate an event
# into a row, and the mapping they do is covered by the plain unit tests.
spring.modulith.events.externalization.enabled=false
spring.kafka.listener.auto-startup=false
